import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  starCountContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
  textContent: {
    color: "#9A9A9A",
    fontSize: 12,
    marginLeft: 4,
  },
});
